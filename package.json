{
  "name": "dialog-fn",
  "version": "1.0.0",
  "repository": "https://github.com/njacob1001/dialog-fn",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "$_ run --stream --parallel build",
    "react": "$_ run --filter react dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "publishConfig": {
    "@njacob1001:registry": "https://npm.pkg.github.com"
  }
}
