{
  "name": "dialog-fn",
  "version": "1.0.0",
  "homepage": "https://github.com/dialog-fn/dialog-fn?tab=readme-ov-file#dialog-fn",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dialog-fn/dialog-fn"
  },
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "$_ run --stream --parallel build",
    "react": "$_ run --filter react dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "publishConfig": {
    "@njacob1001:registry": "https://npm.pkg.github.com"
  }
}
